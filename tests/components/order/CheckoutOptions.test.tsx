import "@testing-library/react-native/extend-expect"
import { render, screen } from "@testing-library/react-native"

describe.skip("<CheckoutOptions />", () => {
  it("shows the current date and time", () => {})
})
